package com.product.bookmyvenue.global;

/**
 * Class file to hold all the constants, whicha are accessible globally.
 */
public class Constants {

    public static final String REGEX_NAME = "^[a-zA-Z ]+$";         // Chars and optional spaces (non empty).
    public static final String REGEX_MOBILE = "^[0-9]{10}$";       // 10 digit mobile only.

}
